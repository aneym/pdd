# PURPOSE

<!--
  This is your project's northstar. Fill each section in plain language — a person
  who has never seen the project should be able to read it top to bottom and
  understand what it is for and how agents should behave inside it.

  Rules for this doc (keep them true as you edit):
    • Plain language. No jargon a newcomer would have to look up.
    • No absolutes. Write defaults and postures, not "never" / "always" bans —
      except where a real, deterministic gate enforces a true invariant.
    • Outcome-driven, never timeline-driven. Describe where you want to get, not by when.
    • Short enough to be read at the start of every session. If it grows past ~2 pages,
      something belongs in the roadmap or the ledger, not here.
  Replace every <angle-bracket> prompt with your own words, then delete these comments.
-->

<One paragraph: what this project makes, and for whom. Lead with the point.
State what "working" looks like — the condition that, if true, means the project
is succeeding. Make it expand ambition, not scope it down.>

Read this before working. This doc is the project's northstar — the shared
understanding every agent starts from. Details about a specific person's
preferences or style live elsewhere; this is what the project itself is for.

## The outcome ladder (no timelines — outcomes only)

<A short, ordered list of outcomes, each a rung the project climbs to. Order by
dependency — later rungs build on earlier ones. No dates.>

1. <the first outcome that must be true>
2. <the next outcome, once the first is solid>
3. <the further-out outcome>

## How this project works — laws for agents

<A handful of short, named laws. Each names a behavior every agent should follow
inside this project, in plain language, as a default or posture. Bold the name,
then one or two sentences. Delete the examples below and write your own.>

**<Law name>.** <What agents should do, and briefly why. A default, not a ban.>

**<Law name>.** <...>

**<Law name>.** <...>

## Boundaries

<Three postures, not a rulebook. Keep them lenient — the owner can always override.>

Always: <the things an agent should just do, without asking>.

Ask first (inline, in the moment): <the things an agent should check before doing>.

Posture, not bans (the owner can always override): <the soft defaults — how things
usually go, stated so an agent knows the norm without being forbidden to deviate>.

## Charters — feature goals under this northstar

<Charters are the few, stable goals of the project's major features. Each says
WHAT the feature is for and WHY (which part of the purpose it serves) — not how
it is built. Keep them few and durable; the volatile "how" lives in the roadmap
as lanes/atoms, each citing the charter it serves.>

- **<Charter name>** — <what this feature is for, in a sentence or two.>
  Serves: <the part of the purpose this advances>.

- **<Charter name>** — <...>. Serves: <...>.

## Unknowns — figure out through building

<It is fine not to know something yet. List the open questions you have chosen to
resolve by building rather than by deciding up front. These are first-class
entries, not failures. Move one up into the doc proper once building answers it.>

- <open question> — <why it stays open; what building will teach us>.

## Changing this doc

Anybody may amend this doc — agents included — as understanding of the project and
the world improves. It is derived, living, and shared by everyone building the
project. Charters hold feature goals here; the roadmap holds the lanes and atomic
work, each citing the charter it serves. When the purpose changes, re-weigh what
gets built next.

Decisions ledger: <path to your decisions-ledger.md>. Authoring rules for this
doc: plain language, no absolutes, defaults over bans, outcome-driven never
timeline-driven, short enough to read every session.
